[mcp_servers.playwright]
command = "npx"
args = [
  "-y",
  "@playwright/mcp@0.0.80",
  "--isolated",
  "--snapshot-mode",
  "full",
  "--save-session",
]
startup_timeout_sec = 30
tool_timeout_sec = 120
