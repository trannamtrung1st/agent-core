{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@0.0.80",
        "--isolated",
        "--snapshot-mode",
        "full",
        "--save-session"
      ]
    }
  }
}
